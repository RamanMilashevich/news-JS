import js from '@eslint/js';
import tsPlugin from '@typescript-eslint/eslint-plugin';
import tsParser from '@typescript-eslint/parser';
import prettierPlugin from 'eslint-plugin-prettier';
import importPlugin from 'eslint-plugin-import';

export default [
  {
    files: ['**/*.js', '**/*.ts'], // Указываем, к каким файлам применяются правила
    ignores: [
      '*.config.js', // Игнорируем все файлы конфигурации
      'webpack*.config.js', // Игнорируем файлы Webpack
      'src/components/controller/appLoader.js', // Игнорируем конкретный файл
    ],
    languageOptions: {
      ecmaVersion: 2020,
      sourceType: 'module',
      parser: tsParser,
    },
    plugins: {
      '@typescript-eslint': tsPlugin,
      prettier: prettierPlugin,
      import: importPlugin,
    },
    rules: {
      'no-debugger': 'off',
      'no-console': 'off',
      '@typescript-eslint/no-explicit-any': 'warn',
      'no-undef': 'off', // Отключаем правило no-undef для глобальных переменных
    },
  },
];